---
import type { Frontmatter } from '@types';
import Layout from '@layouts/Layout.astro';
import { SITE } from '@config';

export const frontmatter: Frontmatter = {
	title: '留言版',
	description: '欢迎在这留下你的足迹，与我分享你的想法和感受。',
	permalink: '/visitorbook',
	layout: 'page',
	showLastModified: false,
};
---

<Layout {...frontmatter} title="留言版 - 灵梦的小站" description={frontmatter.description}>
  <article class="prose prose-zinc max-w-none dark:prose-invert">
    <h1 id="留言板">💬 留言板</h1>
    <p>欢迎来到我的留言板！在这里，你可以：</p>
    <ul>
      <li>分享你的心情和想法</li>
      <li>提出问题或建议</li>
      <li>与我和其他访客交流</li>
      <li>留下你的足迹</li>
    </ul>
    <blockquote>
      <p>每一个留言都是珍贵的记忆，感谢你愿意在这里停留。</p>
    </blockquote>
    <p><strong>温馨提示</strong>：留言时请遵守网络礼仪，互相尊重，共同营造一个温馨的交流空间。</p>
  </article>

  <!-- Twikoo 区域 -->
  <div class="mt-12">
    <h2 class="text-2xl font-bold mb-6 text-center text-gray-800 dark:text-gray-200">
      🌟 访客留言
    </h2>
    <div id="twikoo-visitorbook" class="twikoo-container"></div>
  </div>

  <!-- 样式 -->
  <style>
    .twikoo-container {
      max-width: 800px;
      margin: 0 auto;
      padding: 1.5rem;
      background: var(--card-bg);
      border-radius: 12px;
      box-shadow: var(--card-shadow);
    }
    @media (max-width: 768px) {
      .twikoo-container {
        padding: 1rem;
      }
    }
  </style>

  <!-- 引入并初始化 Twikoo -->
  <script is:inline>
    if (typeof window !== 'undefined') {
      const initTwikoo = () => {
        const script = document.createElement('script');
        script.src = 'https://cdn.jsdelivr.net/npm/twikoo@latest/dist/twikoo.min.js';
        script.onload = () => {
          if (typeof twikoo !== 'undefined') {
            twikoo.init({
              url: 'https://twikoo-api-gmgc.vercel.app', // 使用你的自建API地址
              el: '#twikoo-visitorbook',
              path: '/visitorbook',
              lang: 'zh-CN',
              commentCount: true,
              inputPlaceholder: '说点什么吧～',
              emoji: [
                'https://cdn.jsdelivr.net/gh/IKnowYou233/twikoo-emoji@main/emoji.json',
                'https://cdn.jsdelivr.net/gh/IKnowYou233/twikoo-emoji@main/emoji-akashi.json'
              ],
              dark: document.documentElement.classList.contains('dark')
            }).catch(err => {
              console.error('Twikoo init failed:', err);
            });
          }
        };
        script.onerror = () => {
          console.error('Failed to load Twikoo script');
        };
        document.head.appendChild(script);
      };

      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initTwikoo);
      } else {
        initTwikoo();
      }
    }
  </script>
</Layout>
