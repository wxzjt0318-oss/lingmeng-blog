---
import Layout from '@layouts/Layout.astro';
---

<Layout title="留言版">
  <h1>💬 留言板</h1>
  <p>欢迎在这里分享你的心情、想法或建议。</p>

  <!-- 评论容器 -->
  <div id="twikoo"></div>

  <!-- 直接引入 Twikoo SDK（不使用 import()） -->
  <script src="https://cdn.jsdelivr.net/npm/twikoo@latest/dist/twikoo.all.min.js" defer></script>

  <script>
    // 等待 Twikoo 加载完成
    window.addEventListener('load', () => {
      if (typeof window.twikoo === 'undefined') {
        console.error('Twikoo not loaded!');
        return;
      }

      try {
        window.twikoo.init({
          envId: "https://twikoo-api-gmgc.vercel.app/",
          el: '#twikoo',
          path: '/visitorbook',
          lang: 'zh-CN'
        });
      } catch (err) {
        console.error('Twikoo init error:', err);
      }
    });
  </script>
</Layout>
