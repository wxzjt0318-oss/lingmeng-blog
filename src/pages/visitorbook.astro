---
import Layout from '@layouts/Layout.astro';

export const frontmatter = {
	title: '留言版',
	description: '欢迎留言！',
	permalink: '/visitorbook',
	layout: 'page',
};
---

<Layout title="留言版" description="欢迎留言！">
  <article class="prose max-w-none dark:prose-invert">
    <h1>💬 留言板</h1>
    <p>欢迎留下你的足迹！</p>
  </article>

  <div id="twikoo-visitorbook" class="mt-8"></div>

  <script is:inline>
    (function() {
      if (typeof window === 'undefined') return;
      
      const script = document.createElement('script');
      script.src = 'https://cdn.jsdelivr.net/npm/twikoo@latest/dist/twikoo.min.js';
      script.onload = function() {
        if (typeof twikoo !== 'undefined') {
          twikoo.init({
            url: 'https://twikoo-api-gmgc.vercel.app', // ✅ 关键：无 envId，无结尾斜杠
            el: '#twikoo-visitorbook',
            path: '/visitorbook',
            lang: 'zh-CN'
          }).catch(function(err) {
            console.error('[Twikoo] 初始化失败:', err);
          });
        }
      };
      script.onerror = function() {
        console.error('[Twikoo] 加载脚本失败');
      };
      document.head.appendChild(script);
    })();
  </script>
</Layout>
